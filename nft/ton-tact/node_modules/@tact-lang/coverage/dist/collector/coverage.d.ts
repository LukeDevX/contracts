/// <reference types="node" />
import { Maybe } from "../utils/maybe";
import { LogEntry } from "./parseVMLogs";
type CollectedCell = {
    offsets: Map<number, {
        count: number;
        gas: bigint[];
    }>;
};
export declare class CoverageCollector {
    #private;
    coverageForCell(hash: string): CollectedCell | undefined;
    collectCell(hash: Buffer, offset: number, gas: bigint): void;
    export(): any;
}
export declare function collectCoverage(args: {
    collector: CoverageCollector;
    logs: LogEntry[];
    gasLimit?: Maybe<bigint>;
}): void;
export {};
